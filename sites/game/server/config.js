var config = {}

config.debug = true

config.server_port = 9999
config.server_resource = "s"

config.area = {
    width: 30,
    height: 15
}

config.player = {
    fps: 200
}


module.exports = config