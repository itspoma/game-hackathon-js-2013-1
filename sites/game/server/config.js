var config = {}

config.debug = true

config.server_port = 9999
config.server_resource = "s"

config.area = {
    width: 30,
    height: 9
}

config.player = {
    move_ps: 100,
    shoot_ps: 200,
    shoot_max_alive: 5
}


module.exports = config